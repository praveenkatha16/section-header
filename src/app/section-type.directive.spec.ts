import { SectionTypeDirective } from './section-type.directive';

describe('SectionTypeDirective', () => {
  it('should create an instance', () => {
    const directive = new SectionTypeDirective();
    expect(directive).toBeTruthy();
  });
});
